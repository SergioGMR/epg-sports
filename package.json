{
  "name": "epg-sports",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "start": "bun index.js",
    "combine": "bun run combine.ts",
    "shuffle": "bun run shuffle.ts",
    "run-all": "bun run run-all.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@types/node": "^22.8.6",
    "playwright": "^1.51.1",
    "zod": "^3.24.2"
  },
  "postInstall": "tsc"
}